/**
 * Tool identity enum for all game tools
 */

export enum Tool {
  SELECT = 'select',
  ROAD = 'road',
  BULLDOZE = 'bulldoze',
  ZONE_RESIDENTIAL = 'zone_residential',
  BUILDING = 'building',
  // More tools in MVP-1
}
